export default class Note {

    /**
     * Initializes a Note object.
     * 
     * @param {Number} id identifes the note
     * @param {String} content text of the note
     */
    constructor(id, content) {
        this.id = id;
        this.content = content;
    }
}
